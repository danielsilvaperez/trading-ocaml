# Library Layer

This repository is split into domain-focused top-level libraries (`core`, `venues`, `strategy`, `risk`, `portfolio`, `engine`, `backtest`, `simulation`, `interfaces`).

`lib/` is reserved for shared cross-cutting modules and future umbrella library composition.
